<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sh.nogorcourieradmin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/nogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activity.Analysis" />
        <activity android:name=".Activity.Salary" />
        <activity android:name=".Activity.Login" />
        <activity android:name=".Activity.Today" />
        <activity android:name=".Activity.ToDestination" />
        <activity android:name=".Activity.InWirehouse" />
        <activity android:name=".Activity.Read" />
        <activity android:name=".Activity.Home">

        </activity>
        <activity android:name=".Activity.MainActivity" />
    </application>

</manifest>